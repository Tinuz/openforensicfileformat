OFFF Backlog
============

Hardening Program v0.2 (Forensic-grade)
=======================================

P0 - Direct oppakken
--------------------

- [x] Hardening Sprint 0: repository baseline en docs entrypoint (P0)
  - Add root `README.md` as canonical project entrypoint
  - Add `docs/status.md` with stable/experimental/planned matrix
  - Link root docs to spec + schema catalog + status matrix
  - Added: 2026-05-23
  - Status: done
  - Done: 2026-05-23
  - Progress: root `README.md` toegevoegd met quickstart/build/examples + referenties; `docs/status.md` toegevoegd en gelinkt vanuit root README
  - Acceptance:
    - Root README exists and contains quickstart and architecture overview
    - Status matrix exists and is linked from README

- [ ] Hardening Sprint 1: crash-safe convert finalisatie (P0)
  - Convert writes to temporary output directory (`{output}.tmp-{uuid}`)
  - Write `manifest.json` strictly as finalization point
  - Perform self-check before publish and atomic rename on local FS
  - Add object-store completion marker (`_OFFF_COMPLETE`) for S3 mode
  - Added: 2026-05-23
  - Status: in-progress
  - Progress: `offf-convert` schrijft nu eerst naar temp output, schrijft `manifest.json` als laatste, draait self-check en publiceert via atomic rename; temp output wordt opgeruimd bij fouten
  - Remaining: `_OFFF_COMPLETE` marker voor object storage volgt zodra convert write-pad `s3://` ondersteunt

- [x] Hardening Sprint 2: verify existing chunks before dedup skip (P0)
  - In `write_chunk`, verify stored and plaintext hashes when chunk exists
  - Reuse chunk only on full match; fail-fast on mismatch/corruption
  - Add unit tests for valid existing chunk and corrupt existing chunk
  - Added: 2026-05-23
  - Status: done
  - Done: 2026-05-23
  - Progress: `write_chunk` valideert nu bestaande chunkbestanden cryptografisch voordat dedup skip plaatsvindt; tests toegevoegd voor hergebruik van valide bestaande chunk en fail-fast bij corrupte bestaande chunk

- [x] Hardening Sprint 3: true deterministic mode + sector-size parameter (P0)
  - Add `--sector-size` (default 512), persist in manifest and acquisition params
  - Make deterministic timestamps and event IDs stable for identical input
  - Ensure repeated deterministic runs produce byte-equivalent metadata
  - Added: 2026-05-23
  - Status: done
  - Done: 2026-05-23
  - Progress: `offf-convert` ondersteunt nu `--sector-size`; `manifest.source.sector_size` en `acquisition.parameters.sector_size` gebruiken deze waarde; deterministic mode gebruikt vaste timestamp (`1970-01-01T00:00:00Z`) en stabiele provenance output zodat repeated deterministic runs byte-equivalente `manifest.json`, `acquisition.json` en provenance opleveren

- [x] Hardening Sprint 4: Merkle proofs + full tree validation (P0)
  - Add inclusion proof generate/verify functions in `offf-core`
  - Add CLI interface for proof generation/verification
  - Validate `merkle_tree.bin` structure (magic, version, lengths, levels, root consistency)
  - Added: 2026-05-23
  - Status: done
  - Done: 2026-05-23
  - Progress: `offf-core::hash` bevat nu `generate_merkle_proof`, `verify_merkle_proof` en volledige `merkle_tree.bin` structuurvalidatie met level/internal-node checks; `offf-verify` gebruikt deze validatie, vergelijkt leaves met `physical_to_chunk` volgorde, en ondersteunt `--proof-chunk` voor proof generatie + verificatie

- [x] Hardening Sprint 5: verifier profiles + leaves consistency (P0)
  - Add profiles: `core`, `core+schemas`, `core+extensions`, `conformance`
  - Add machine-readable JSON report output
  - Validate `hashes/leaves.parquet` against `maps/physical_to_chunk.parquet`
  - Extend provenance validation (required fields, uniqueness, invalid JSONL detection)
  - Added: 2026-05-23
  - Status: done
  - Done: 2026-05-23
  - Progress: `offf-verify` ondersteunt nu `--profile` + `--report`; conformance-profiel schrijft machine-readable JSON rapport; `leaves.parquet` wordt sequence/hash-gewijs vergeleken met `physical_to_chunk`; provenance checks valideren JSON parsebaarheid, verplichte velden, unieke `event_id` en monotone `evt-` ordering

- [x] Hardening Sprint 6: append-only analysis model (P0)
  - Move worker outputs to `analysis/jobs/{job_id}/...`
  - Add per-job `result_manifest.json` with output artifact hashes
  - Add correction events as append-only records (no in-place mutations)
  - Added: 2026-05-24
  - Status: done
  - Done: 2026-05-24
  - Progress: `offf-keyword-worker` en `offf-yara-worker` schrijven nu naar `analysis/jobs/{job_id}/...`, weigeren overschrijven van bestaande job-artifacts, en schrijven per job een `result_manifest.json` met SHA-256 van outputbestanden; provenance verwijst naar output + result manifest. `offf-access-service` ondersteunt append-only correction events via REST (`/analysis-corrections`) en gRPC (`AppendAnalysisCorrection`) naar `analysis/events/analysis_events.jsonl`

- [x] Hardening Sprint 7: Access Service production auth + denied audit (P0)
  - Add `OFFF_AUTH_MODE=dev_headers|jwt|mtls`
  - Enforce trusted role/tool identity from claims/cert in production modes
  - Persist denied writes to append-only denied event log
  - Harden write path validation (no evidence layer writes, no traversal, no overwrite)
  - Added: 2026-05-24
  - Status: done
  - Done: 2026-05-24
  - Progress: `offf-access-service` ondersteunt nu `OFFF_AUTH_MODE` met mode-specifieke trusted identity headers/metadata (`dev_headers`, `jwt`, `mtls`), logt denied write attempts append-only naar `extensions/access/denied_access_events.jsonl`, en hardent write targets naar `analysis/jobs/{job_id}/...` met traversal- en overwrite-blokkering. gRPC smoke-tests zijn bijgewerkt en valideren denied overwrite logging

- [x] Hardening Sprint 8: CI + CLI E2E + negative conformance datasets (P0)
  - Enforce `cargo fmt --check`
  - Enforce `cargo clippy --workspace --all-targets -- -D warnings`
  - Enforce `cargo test --workspace` and release build
  - Add end-to-end CLI workflows and negative datasets with machine-readable outcomes
  - Added: 2026-05-24
  - Status: done
  - Done: 2026-05-24
  - Progress: `.github/workflows/offf-ci.yml` bevat nu harde quality gates (`fmt`, strikte `clippy`, volledige workspace tests en release build), plus aparte jobs voor conformance en lokale CLI E2E met artifact upload van JSON-rapporten. `tests/e2e/run_cli_e2e.py` voegt een lokale convert->verify->keyword->yara keten toe met machine-readable output (`tests/e2e/cli-e2e-report.json`). Conformance negatieve scenario's zijn dataset-gedreven gemaakt via `tests/conformance/negative_cases.json` en worden machine-readable gerapporteerd

- [ ] Lineage Sprint 9: object lineage core model + schemas (P0)
  - Add generic OFFF Core row models:
    - `DiscoveredObjectRow` / object index row
    - `ObjectEdgeRow`
    - `DerivationRow`
    - `ObjectSourceRef` / `ObjectStorageRef`
  - Add schema set in `docs/schema`:
    - `offf-object-index-row-0.2.0.schema.json`
    - `offf-object-edge-row-0.2.0.schema.json`
    - `offf-derivation-row-0.2.0.schema.json`
    - `offf-derived-object-store-0.2.0.schema.json`
    - `offf-lineage-report-0.2.0.schema.json`
    - `offf-object-producing-result-manifest-0.2.0.schema.json`
  - Add object indexes locations + IO:
    - `indexes/objects/object_index.parquet`
    - `indexes/objects/object_edges.parquet`
    - `indexes/objects/derivations.parquet`
  - Add lineage validator primitives (`ObjectLineageValidator`) for referential integrity checks
  - Added: 2026-05-24
  - Status: in-progress
  - Progress: `offf-core` bevat nu generieke object-lineage modellen (`DiscoveredObjectRow`, `ObjectEdgeRow`, `DerivationRow`, `ObjectSourceRef`, `ObjectStorageRef`), Parquet read/write helpers voor `indexes/objects/*`, en validator-primitieven via `ObjectLineageValidator` (referential checks + cycle detectie). `JobManifest` is v0.2-voorbereid met optionele `input_scope` en `output_contract` zonder v0.1-compat te breken. In `docs/schema` zijn de gevraagde 0.2.0 lineage/object-producing schema’s toegevoegd plus een 0.2 catalog.
  - Acceptance:
    - `object_index`, `object_edges`, `derivations` schema-validated and loadable
    - parent/child references validated; missing refs reported
    - cycle detection available (error or warning by profile)

- [ ] Lineage Sprint 10: derived object store + worker output contract v0.2 (P0)
  - Implement content-addressed derived object store:
    - `derived/objects/sha256/ab/cd/<sha256>.bin`
    - storage modes: `referenced_only` and `materialized`
    - verify existing bytes on hash collision/reuse
  - Extend `JobManifest` with generic `output_contract` capabilities
  - Extend result manifest for object-producing outputs:
    - `object_index_delta`, `object_edges_delta`, `derivations_delta`, `materialized_objects`
    - output hashes required for all artifacts
    - `result_manifest.json` written last
  - Extend worker output directory contract under `analysis/jobs/{job_id}/...`
  - Added: 2026-05-24
  - Status: done
  - Done: 2026-05-25
  - Progress: `offf-core::storage` bevat nu `derived_object_path`, `write_derived_object` (hash-verify op reuse) en `read_derived_object`; `offf-keyword-worker` en `offf-yara-worker` gebruiken v0.2 result manifest met `status`, `statistics`, `errors.jsonl` collectie via error-tolerante scan (for_each/match ipv try_for_each); `offf-jobs` ondersteunt nu `create-object-worker` subcommand met volledige `output_contract` vlaggen
  - Acceptance:
    - workers can produce results-only or object-producing outputs via one contract
    - materialized objects never written to evidence/chunks layer
    - result manifest is finalization point and hash-complete

- [x] Lineage Sprint 11: SDK + Access API for object-producing jobs (P0) ✅ 2026-05-26
  - Add SDK APIs:
    - `write_object_delta`, `write_edge_delta`, `write_derivation_delta`
    - `materialize_derived_object`
    - verified object reads and lineage reads
  - Add Access Service endpoints (append-only, job-scoped):
    - `POST /cases/{caseId}/analysis/jobs/{jobId}/objects`
    - `POST /cases/{caseId}/analysis/jobs/{jobId}/object-edges`
    - `POST /cases/{caseId}/analysis/jobs/{jobId}/derivations`
    - `POST /cases/{caseId}/analysis/jobs/{jobId}/materialized-objects`
    - object graph reads (`/objects`, `/children`, `/parents`, `/lineage`, `/content`)
  - Extend tool registry capabilities for object-producing workers
  - Enforce capability vs `output_contract` checks at write time
  - Added: 2026-05-24
  - Status: in-progress
  - Progress: gestart 2026-05-25
  - Acceptance:
    - writes to `indexes/objects/*` directly are denied
    - denied writes are audited append-only
    - object-producing worker mock passes integration tests

- [x] Lineage Sprint 12: deterministic object-index rebuild + lineage verify CLI (P0) ✅ 2026-05-26
  - Added: 2026-05-24
  - Status: done
  - Progress: completed 2026-05-26
  - Add `offf-index objects <case>` rebuild from job deltas:
    - read all job deltas
    - validate schema
    - deduplicate object conflicts deterministically
    - rebuild central indexes under `indexes/objects/`
  - Add `offf-verify <case> --object <obj_id> --lineage`
  - Validate lineage chain to root evidence/chunks + provenance refs
  - Add conformance datasets for valid and invalid object graphs
  - Added: 2026-05-24
  - Status: planned
  - Acceptance:
    - lineage valid/invalid outcomes machine-readable in verify report
    - missing parent/derivation/hash mismatch detected
    - index rebuild deterministic and reproducible

Lineage Implementatievolgorde Per Crate (S9-S12)
-------------------------------------------------

- Wave 1 - `offf-core` + schema baseline (Sprint 9)
  - `crates/offf-core/src/types.rs`:
    - add `DiscoveredObjectRow`, `ObjectEdgeRow`, `DerivationRow`
    - add `ObjectSourceRef`, `ObjectStorageRef`
    - extend `JobManifest` with `output_contract` + input scope selectors (v0.2)
  - `crates/offf-core/src/parquet_io.rs`:
    - add read/write helpers for object rows, edges, derivations
  - `docs/schema/`:
    - add v0.2 object-lineage schemas from instructies
  - `crates/offf-core/src/lib.rs`:
    - export new object-lineage modules/types

- Wave 2 - worker contract + derived store (Sprint 10)
  - `crates/offf-core/src/storage.rs`:
    - add content-addressed derived object read/write verify helpers
    - enforce immutable verify-on-reuse behavior
  - `crates/offf-keyword-worker/src/main.rs` and `crates/offf-yara-worker/src/main.rs`:
    - add `errors.jsonl` emission and result manifest v0.2 output categories
    - keep result-producing flow backward compatible
  - `crates/offf-jobs/src/main.rs`:
    - support creating v0.2 job manifests with `output_contract`

- Wave 3 - access/service + sdk write path (Sprint 11)
  - `crates/offf-access-service/src/main.rs` + `crates/offf-access-service/proto/offf_access.proto`:
    - add append-only endpoints/rpcs for objects, object-edges, derivations, materialized-objects
    - block direct writes to `indexes/objects/*`
    - enforce capability checks against tool registry + `output_contract`
  - `config/tool-registry.example.json`:
    - add object-producing capabilities matrix
  - `sdk/python/offf_sdk/`:
    - add SDK methods for object deltas, lineage reads, derived object materialization

- Wave 4 - merge/rebuild + lineage verify (Sprint 12)
  - `crates/offf-index/src/main.rs`:
    - add `objects` subcommand for deterministic rebuild from job deltas
  - `crates/offf-verify/src/main.rs`:
    - add `--object <id> --lineage` path validation
    - validate object graph consistency, derivation chain, provenance refs, derived object hashes
  - `tests/conformance/` + `tests/integration/`:
    - add positive/negative object-graph datasets and lineage failure cases

- Cross-cutting Definition of Done gates
  - CI must pass: `fmt`, strict `clippy`, workspace tests, release build
  - Each wave adds conformance + integration coverage before next wave starts
  - No direct mutation of evidence layer (`chunks/`, `hashes/`, `maps/`, `manifest.json`, `acquisition.json`)

P1 - Daarna
-----------

- [ ] OFFF v0.2 manifest extensions foundation (P1)
  - Add optional `extensions` section to `ManifestJson`
  - Add v0.2 schema catalog entries and compatibility tests for v0.1 readers

- [ ] Object graph read/query APIs (P1)
  - Add list/get/children/parents/lineage endpoints and SDK wrappers
  - Add graph pagination/filtering for large cases
  - Add machine-readable lineage report export

- [ ] Legacy compatibility profile for analysis outputs (P1)
  - Keep v0.1 result layouts readable with verifier warnings
  - Mark legacy workers/results as non-forensic-grade where required
  - Provide migration guidance for v0.2 output contract adoption

- [ ] Generic extension types + append-only APIs (P1)
  - Add core structs and schemas: labels, scopes, sets, decisions, policy refs, access/denied/audit events
  - Add Access Service REST/gRPC append-only extension endpoints
  - Add validator checks for extension schema and referential integrity

- [ ] Scope-aware jobs and workers (P1)
  - Extend `JobManifest` with `scope_ref`, `include_sets`, `exclude_sets`, `exclude_labels`
  - Enforce effective scope in workers and emit audit events for skipped items

- [ ] Object-per-event append model for object storage (P1)
  - Replace JSONL read-modify-write patterns with immutable event objects
  - Provide index rebuild path from event objects to derived JSONL indexes

- [ ] Indexing hardening GPT/MBR and worker quality upgrades (P1)
  - GPT CRC + backup GPT checks
  - Extended MBR/EBR support
  - file_id resolution for hits and chunk-boundary matching support

- [ ] Python SDK hardening and forward compatibility (P1)
  - Bounded LRU chunk cache and cache disable option
  - Append-only analysis writer APIs (job-specific)
  - Version profile support for 0.1.x and 0.2.x with safe extension ignore

P2 - Later
----------

- [ ] Object lineage scale/performance program (P2)
  - Streaming reads for large derived objects
  - Graph export formats and offline lineage reports
  - Performance tuning for millions of objects/edges/derivations
  - Memory-bounded merge/rebuild strategy for object indexes

- [ ] Threat model and security mapping (P2)
  - Add `docs/threat-model.md` with threat-to-mitigation-to-test traceability

- [ ] Versioning policy and migration path (P2)
  - Add `docs/versioning.md`
  - Add `offf-migrate` path from v0.1 to v0.2 with evidence hash invariants

- [ ] NTFS forensic depth improvements (P2)
  - ATTRIBUTE_LIST support
  - ADS/sparse/compressed/encrypted modeling
  - Streaming parser memory hardening

- [x] OFFF packed container (single-file) initial implementation (P1)
  - Add core packed container format with deterministic index + checksum footer
  - Add pack/list/unpack operations for `.offfpack`
  - Keep exploded directory model as canonical representation
  - Owner: next implementation cycle
  - Added: 2026-05-23
  - Status: done
  - Done: 2026-05-23
  - Progress: `offf-core::packed` toegevoegd met `pack_directory`, `read_index`, `unpack_to_directory` + tests; `offf-export` uitgebreid met `pack`, `list`, `unpack`, `export`

- [x] Phase 5: MinIO end-to-end smoke test
  - Validate `offf-verify` against s3:// URI on MinIO
  - Validate `offf-keyword-worker` and `offf-yara-worker` against remote container
  - Validate remote provenance append behavior under concurrent worker writes
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Status: done
  - Done: 2026-05-23
  - Progress: `tests/smoke/phase5_minio_smoke.py` valideert `offf-verify` tegen `s3://` op MinIO, draait `offf-keyword-worker` + `offf-yara-worker` tegen remote container, en controleert concurrente provenance appends

- [x] Phase 7: E01 smoke test + ewfexport installatie
  - Install libewf tools and ensure `ewfexport` is available in PATH
  - Verify with `Get-Command ewfexport` that command resolution succeeds
  - Convert a real `.E01` sample with `offf-convert --input-type e01`
  - Confirm `acquisition.json` contains:
    - `source_container.type = E01`
    - `source_container.container_sha256`
    - `source_container.tool_used = ewfexport`
    - `evidence_stream.stream_sha256`
  - Run `offf-verify` on the generated OFFF container and expect VALID
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Status: done
  - Done: 2026-05-23
  - Progress: `tests/smoke/phase7_e01_smoke.py` genereert een `.E01`, converteert met `offf-convert --input-type e01`, valideert `acquisition.json` velden en verifieert de output met `offf-verify`

- [x] SDK: contract-test voor minimale Python API + CI guard
  - Add `sdk/python/tests/test_api_contract.py`
  - Assert presence of required API names:
    - `open_container`
    - `read_manifest`
    - `verify_container`
    - `read_chunk`
    - `verify_chunk`
    - `map_offset_to_chunk`
    - `read_file_index`
    - `write_analysis_result`
    - `append_provenance_event`
  - Add CI step to run this contract-test on every PR
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Done: 2026-05-22

- [x] Access Service: gRPC smoke test + CI gate (P0)
  - Add an end-to-end smoke test for gRPC methods in `offf-access-service`:
    - `GetManifest`
    - `GetChunk`
    - `VerifyChunk`
    - `ListFiles`
    - `GetFile`
    - `ListArtifacts`
    - `WriteAnalysisResults`
    - `AppendProvenanceEvent`
  - Use `tests/samples/4orensics.case2.offf` as baseline case data
  - Add CI step so the smoke test is mandatory on PRs
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Done: 2026-05-22

- [x] Access Service: capability model enforcement (P0)
  - Implement authn/authz and role-based write restrictions
  - Enforce layer policy:
    - evidence immutable
    - analysis append-only
    - provenance append-only
    - index writes only by approved profile
  - Add audit logging for denied/allowed writes
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Done: 2026-05-22

- [x] Access Service: storage backends parity (P0)
  - Extend case resolution beyond local filesystem
  - Add S3/MinIO/Ceph object-store support in access-service read/write flows
  - Add parity tests local vs s3:// case paths
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Status: done
  - Done: 2026-05-23
  - Progress: access-service gebruikt nu `ContainerRef` voor local + `s3://` case-resolutie en read/write flows (manifest/chunk verify, file index, artifacts, analysis append, provenance append); parity smoke test toegevoegd in `crates/offf-access-service/tests/grpc_storage_parity.rs`

- [x] Conformance suite + integration profiles (P0)
  - Create official PASS/FAIL conformance suite for:
    - Reader profile
    - Analysis profile
    - Indexer profile
    - Acquisition profile
  - Include negative datasets (corrupt chunk, missing chunk, invalid provenance, bad offsets)
  - Publish machine-readable result report format
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Done: 2026-05-22
  - Progress: `tests/conformance/run_conformance.py` now evaluates reader/analysis/indexer/acquisition + negative scenarios

- [x] CI baseline voor OFFF ecosysteem (P0)
  - Add repository CI workflows under `.github/workflows/`
  - Minimum required jobs:
    - rust build/test for core crates
    - access-service REST + gRPC smoke tests
    - Python SDK contract test
    - schema validation checks
  - Owner: next implementation cycle
  - Added: 2026-05-22
    - Done: 2026-05-22

- [x] Go SDK minimal profile implementation (P1)
  - Create `sdk/go` with API parity for minimum OFFF SDK surface
  - Include container open/read/verify + analysis/provenance append APIs
  - Add smoke tests against sample OFFF container
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Status: done
  - Done: 2026-05-23
  - Progress: `sdk/go` toegevoegd met minimale API (`OpenContainer`, `ReadManifest`, `VerifyContainer`, `ReadChunk`, `VerifyChunk`, `MapOffsetToChunk`, `ReadFileIndex`, `WriteAnalysisResult`, `AppendProvenanceEvent`) + smoke test `sdk/go/sdk_test.go` en CI job `go-sdk-smoke`

- [x] Tool registry + governance artifacts (P1)
  - Define schema and storage for approved tool registry
  - Track tool identity, vendor, executable/image hash, allowed profile, OFFF version support
  - Integrate registry checks in Access Service write paths
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Done: 2026-05-22

- [x] Worker framework hardening (P1)
  - Add deterministic job replay support
  - Add retry/failure policy with bounded retries and terminal states
  - Add worker health registry and assignment audit trail
  - Owner: next implementation cycle
  - Added: 2026-05-22
  - Done: 2026-05-22
  - Progress: `offf-jobs run` now writes runtime state + replay id in `jobs/runtime/*.state.json`, assignment audit in `jobs/runtime/assignment_audit.jsonl`, and worker health in `jobs/runtime/worker_health.jsonl`
